# Cliente Mensajería LAN

## Ejecutar
- Editar `config.ini` o copiar `config.ini.sample` -> `config.ini`.
- Ejecutar:
```bash
python app.py
```

## Generar EXE
```bash
pyinstaller --onefile --windowed app.py --name MensajeriaLAN
```
